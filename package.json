{
  "name": "gsc-2026-smart-resource-allocation",
  "version": "1.0.0",
  "description": "Smart Resource Allocation project for GSC 2026",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run start --workspace=apps/web-client",
    "dev:api": "npm run dev --workspace=apps/api-server",
    "build:web": "npm run build --workspace=apps/web-client",
    "build:api": "npm run build --workspace=apps/api-server"
  }
}
